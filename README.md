# Learnworlds SDK

API client, generated from the OpenAPI spec.

The public API of LearnWorlds

Learn more about API at [stoplight.io](https://stoplight.io).

This is an unofficial SDK for the Learnworlds public API, generated by Voxgig with [`@voxgig/sdkgen`](https://github.com/voxgig/sdkgen). It is not affiliated with, endorsed by, or sponsored by the upstream API provider.

Learn more about Voxgig SDKs at [voxgig.com/sdk](https://voxgig.com/sdk/).

> TypeScript, Seneca Provider SDKs, and an interactive REPL — all generated from one OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).

## Entities, not endpoints

This SDK exposes the API as **42 semantic entities** that you
call directly, instead of assembling URL paths and query strings. See the [Entities](#entities) table below for the full list. Entities are
**Capitalised** to mark them as the primary surface, each with the operations they
support (`list`, `load`, `create`, `update`, `remove`):

```ts
const client = new LearnworldsSDK()
```

Thinking in entities keeps the mental model small — for people and AI agents alike —
rather than reasoning about raw HTTP routes and query parameters.

## Offline unit testing

Every SDK ships a built-in **test mode** that swaps the HTTP transport for
an in-memory mock, so your unit tests run fully offline — no server, no
network, and no credentials:

### TypeScript

```ts
const client = LearnworldsSDK.test()
const assessments = await client.Assessment().list()
// assessments is an array of bare Assessment records populated with mock data
console.log(assessments)
```

## Packages

| Language | Package | Install |
| --- | --- | --- |
| TypeScript | `@voxgig-sdk/learnworlds` | publish pending — [install from git tag](https://github.com/voxgig-sdk/learnworlds-sdk/releases) |
| Seneca Provider | `voxgig-sdk-learnworlds` | `not yet on the registry — install from the git tag: https://github.com/voxgig-sdk/learnworlds-sdk/releases` |

## Quickstart

### TypeScript

```ts
import { LearnworldsSDK } from '@voxgig-sdk/learnworlds'

const client = new LearnworldsSDK()


// Load a specific unitanalytics (returns a UnitAnalytics)
const unitanalytics = await client.UnitAnalytics().load({
  course_id: 'example_course_id',
  id: 'example_id',
})
console.log(unitanalytics)
```

See the [TypeScript README](ts/README.md) for the full guide.

## Surfaces

| Surface | Path |
| --- | --- |
| **SDK** (TypeScript, Seneca Provider) | `ts/` `seneca_provider/` |

## Entities

The API exposes 42 entities:

| Entity | Description | API path |
| --- | --- | --- |
| **Active** | The Active entity. | `` |
| **Affiliate** | The Affiliate entity (create, list). | `/v2/affiliates/{id}` |
| **Assessment** | The Assessment entity (list). | `/v2/forms/{id}/responses` |
| **Bundle** | The Bundle entity (list, load). | `/v2/bundles` |
| **ByProduct** | The ByProduct entity. | `` |
| **BySegment** | The BySegment entity. | `` |
| **Calendar** | The Calendar entity (list). | `/v2/school/events` |
| **Certificate** | The Certificate entity (list, remove, update). | `/v2/certificates` |
| **Community** | The Community entity (create, list, remove). | `/v2/community/spaces/{id}/users` |
| **CommunityPost** | The CommunityPost entity (load). | `/v2/community/posts/{id}` |
| **CommunitySpace** | The CommunitySpace entity (create, load, update). | `/v2/community/spaces` |
| **Completed** | The Completed entity. | `` |
| **Coupon** | The Coupon entity (create). | `/v2/promotions/{pid}/coupons` |
| **CouponUsage** | The CouponUsage entity (list). | `/v2/promotions/{pid}/coupons/{cid}/usage` |
| **Course** | The Course entity (create, list, load, update). | `/v2/courses` |
| **CourseAnalytics** | The CourseAnalytics entity (load). | `/v2/courses/{id}/analytics` |
| **CourseContent** | The CourseContent entity (create, list). | `/v2/courses/{id}/sections` |
| **Due** | The Due entity. | `` |
| **Event** | The Event entity. | `` |
| **EventLog** | The EventLog entity (list). | `/v2/event-logs` |
| **Form** | The Form entity. | `` |
| **Installment** | The Installment entity (list). | `/v2/installments/active` |
| **Lead** | The Lead entity (list). | `/v2/leads` |
| **MultipleSeat** | The MultipleSeat entity (create, list, remove). | `/v2/seats/{id}/users/{uid}` |
| **Payment** | The Payment entity (list, load). | `/v2/payments` |
| **Post** | The Post entity. | `` |
| **Promotion** | The Promotion entity (create, list, load). | `/v2/promotions` |
| **Reporting** | The Reporting entity (list). | `/v2/users/{id}/progress` |
| **Score** | The Score entity. | `` |
| **Seat** | The Seat entity (create, load, update). | `/v2/seats` |
| **Segment** | The Segment entity. | `` |
| **Space** | The Space entity. | `` |
| **SubscriptionPlan** | The SubscriptionPlan entity (list, load). | `/v2/subscription-plans` |
| **Unit** | The Unit entity. | `` |
| **UnitAnalytics** | The UnitAnalytics entity (load). | `/v2/courses/{id}/units/{uid}/analytics` |
| **Upcoming** | The Upcoming entity. | `` |
| **UpdateUserProgress** | The UpdateUserProgress entity (create). | `/v2/users/{id}/courses/{cid}/complete` |
| **User** | The User entity (create, list, load, remove, update). | `/v2/assessments/scores/{id}/review` |
| **UserGroup** | The UserGroup entity (create, list, load, update). | `/v2/user_groups` |
| **UserProgress** | The UserProgress entity (list). | `/v2/users/{id}/courses/{cid}/progress` |
| **UserRole** | The UserRole entity (list). | `/v2/user-roles` |
| **UserSubscription** | The UserSubscription entity (list). | `/v2/user-subscriptions` |

The operations available across these entities are **load**, **list**, **create**, **update**, **remove** — see each entity's
own list above for exactly which it supports.

## Quickstart in other languages

## Direct and prepare

For endpoints the entity model doesn't cover, use the low-level methods:

- **`direct(fetchargs)`** — build and send an HTTP request in one step.
- **`prepare(fetchargs)`** — build the request without sending it.

Both accept a map with `path`, `method`, `params`, `query`,
`headers`, and `body`. See the [How-to guides](#how-to-guides) below.

## How-to guides

### Make a direct API call

When the entity interface does not cover an endpoint, use `direct`:

**TypeScript:**
```ts
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

## Advanced

> Everyday use only needs the sections above. This explains the internals
> behind every call — relevant when writing custom features.

Every SDK call runs the same five-stage pipeline:

1. **Point** — resolve the API endpoint from the operation definition.
2. **Spec** — build the HTTP specification (URL, method, headers, body).
3. **Request** — send the HTTP request.
4. **Response** — receive and parse the response.
5. **Result** — extract the result data for the caller.

A feature hook fires at each stage (e.g. `PrePoint`, `PreSpec`,
`PreRequest`), so features can inspect or modify the pipeline without
forking the SDK.

### Features

| Feature | Purpose |
| --- | --- |
| **TestFeature** | In-memory mock transport for testing without a live server |

Pass custom features via the `extend` option at construction time.

## Per-language documentation

- [TypeScript](ts/README.md)
- [Seneca Provider](seneca_provider/README.md)

## Upstream API

This SDK is generated from the upstream OpenAPI specification. It is an
unofficial client and is not affiliated with the API provider.

- Upstream API: [https://stoplight.io/mocks/learnworlds/api:main/2951998](https://stoplight.io/mocks/learnworlds/api:main/2951998)

## Security

Please report security issues to security@voxgig.com. See [SECURITY.md](SECURITY.md).
Do not open public issues for suspected vulnerabilities.

---

Generated from the API OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).
